import XCTest
@testable import swiftui_search_field_shell_line

final class swiftui_search_field_shell_lineTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(swiftui_search_field_shell_line().text, "Hello, World!")
    }
}
